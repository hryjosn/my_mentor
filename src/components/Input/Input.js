import React from 'react';
import { TextField } from '@material-ui/core';

const MyInput = (props) => {
    return (
        <TextField
            {...props}
        />
    );
};

export default MyInput;
